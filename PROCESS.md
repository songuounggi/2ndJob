# 작업 절차서 · 검수 절차서 — 모든 상품, 모든 방 (2026-09-26 사용자 확정)

**새 상품을 만들거나 판을 고칠 때 이 순서를 건너뛰지 않는다.** 각 단계는 "통과 조건"을 채워야 다음으로 간다.
계기: 상품 3 에서 몇 시간 검수를 돌리면서 **기획서 대조를 한 번도 안 했다.** 사용자가 iPad 에서 줄 간격·
탭 순서·겹침을 하나씩 찾았다. "당연히 할 줄 알았던" 기본을 절차로 박는다.

```
1 시장 조사 → 2 기획서 → 3 디자인 → 4 제작(PDF) → 5 검수(기획·디자인·논리·수치) → 6 직접 써 보기 → 7 iPad 검수 → 8 출시
```

| 단계 | 누가 | 만드는 것 | **통과 조건** (이게 없으면 다음 단계 금지) |
|---|---|---|---|
| **1 시장 조사** | Claude (etsy-research 에이전트) | `product<N>-research.md` | 실측 숫자(판매·가격·리뷰)와 출처. 추정은 추정이라고 적는다 |
| **2 기획서** | Claude 초안 → **사용자 확정** | `product<N>-content.md`: 타겟·콘셉트·**페이지 지도(순서·칸·링크)**·문구 원칙 | 사용자 확정. 확정 뒤 바뀐 것은 날짜와 함께 기록 |
| **3 디자인** | **사용자** (또는 사용자에게 받는다) | 핸드오프 / 시안 | 사용자 확정. **디자인은 사용자 것 -- 상의 없이 건드리지 않는다** |
| **4 제작** | Claude | 빌드 스크립트 · PDF | 버전 폴더에 새로(**덮어쓰기 금지**). 표본(몇 장)으로 먼저 보고, 전체 빌드는 한 번에 |
| **5 검수** | Claude (검수 에이전트) | 검수 보고서 | 아래 **검수 절차서 5-1~5-5 전부 0** (예외는 사용자 확정분만) |
| **6 직접 써 보기** | Claude | 사용 시나리오 보고 | 아래 6절 시나리오 전부 끝까지 간다 |
| **7 iPad 검수** | **사용자** (시점마다 필요할 때) | 확인 결과 | Claude 가 **파일 카드 + 볼 페이지 번호 + 볼 것**을 준다. 확인 결과는 문서에 기록 |
| **8 출시** | 사용자 + Claude | Etsy 등록 | `RELEASE.md` 를 처음부터. 올린 판은 `upload/<버전>/`, `shop.md` 이력표 |

---

## 검수 절차서 (5단계) — 순서대로, 전부

| # | 검수 | 묻는 것 | 어떻게 (도구) | 상품 3 예 |
|---|---|---|---|---|
| **5-1** | **기획서 대조** | **기획한 것을 만들었나.** 페이지가 다 있나, 순서, 칸, 문구, 링크 | 기획서 페이지 지도 ↔ 빌드 결과를 절별로. 빌드마다 도는 검사로 만든다 | `scripts/p3/check_plan_p3.py` |
| **5-2** | **디자인** | 핸드오프·확정안과 같은가. 줄 간격·라벨 간격·표 너비·정렬·겹침·잘림 | PDF 에서 직접 잰다(화면과 PDF 배치가 다를 수 있다) + 렌더해서 눈으로 | `audit_pitch_p3` · `audit_layout_p3` · `audit_pdf_overlap_p3` · (공용 `qa_pdf.py` 작업 중) |
| **5-3** | **논리** | 링크가 맞는 곳으로 가나, 빠진 링크, 탭 표시·넘기는 순서, 날짜·요일·주 번호, 들어올 길 없는 페이지 | 완성 PDF 링크 전수 + 구조 검사 | `pdf_independent_check` · 빌드 내 탭 순서 검사 |
| **5-4** | **수치·문구** | 쪽 수·크기·개수·가격이 문서·리스팅과 같나, 오타·미국식 표기·금지 표현 | 리스팅 대조 + 문구 검사 + 교정 | `check_listing_p3.py` · `p3_content.check()` |
| **5-5** | **뷰어·용량** | GoodNotes 위험(그라데이션·반투명·타일), 렌더 속도, 폰트, 20MB | `scripts/check_render.py` + 폰트 검사 | |

**규칙**
- 검사는 **고치기 전 판에서 걸리고, 고친 판에서 0** 인 것을 확인하고 쓴다 (검사가 헛돌지 않게)
- 사용자가 찾은 결함은 **그 자리에서 검사로 만든다** (`CLAUDE.md` "고친 것에는 검사를 붙인다")
- 의도한 예외는 **사용자 확정분만**, 문서에 이유와 함께 적는다
- 검수가 하나라도 FAIL 이면 사용자에게 "완료"라고 보내지 않는다

## 6. 직접 써 보기 (Claude) — 구매자처럼 끝까지

완성 PDF 를 열어 **링크를 실제로 따라가며** 아래를 끝까지 간다. 막히거나 이상한 곳은 페이지 번호로 적는다.
1. 처음 연 사람: 표지 → 넘기기 → 사용법 → 목차 → 아무 도구
2. 오늘 쓰기: YEAR → 오늘 날짜 → 일간 → Tomorrow → 다음 날 → 30일 뒤 메모 도착
3. 한 주·한 달: 주간 → 실험 → 금요일 판정 → 월 계획 → 월 리뷰 → 분기 Keep/Drop
4. 막혔을 때: SOS → 상황 → 도구 → 탭으로 돌아오기
5. 넘기기만: 모든 탭 구역을 넘기면서 탭 표시가 순서대로 흐르는지
6. 연말: 12월 → Mailbox → Playbook → Year review

## 7. iPad 검수 — 사용자에게 줄 것

- **파일 카드**(맨 아래에, 버전·쪽 수 명시) + **볼 페이지 번호와 볼 것 목록**. 폰에서 보기 좋게(그림은 크게, 한 장씩)
- 바뀐 곳은 **전/후 그림**으로
- 결과는 상품 문서의 체크리스트에 날짜와 기기·앱과 함께 기록(아이폰 미리보기는 GoodNotes 확인이 아니다)

## 보고 규칙 (모든 단계)

- 결정이 필요하면 **그림을 먼저** 보여 주고 묻는다. 말로만 설명하지 않는다
- 페이지는 **번호와 이름**으로. "~한 곳"이 아니라 "543쪽 Year-end mailbox"
- 디자인에 영향 주는 변경은 상의 후. 내용·논리 수정은 고치고 보고
