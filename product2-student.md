# 두 번째 상품 — ADHD 학생용 플래너 (기획)

**결정일:** 2026-09-21
**상태:** 제작 중 — v0.1 재배분 작업 **진행 중**(2026-09-23 저녁 중단).
이어서 할 일은 문서 맨 끝 '2026-09-23 인수인계' 절을 먼저 볼 것
**디자인:** 다크 오로라 표지 + 유리 내지로 확정(아래 '디자인 확정' 절).
먹선·수묵 컨셉은 보류

상품 1은 `shop.md` / `listing.md`, 제작 규칙은 `CLAUDE.md`.

---

## 왜 이것인가

리스팅 1개로는 검색 노출이 없다. 두 번째가 필요하다는 데는 이견이 없었지만,
**"리스팅 개수를 늘린다"는 논리는 채택하지 않았다.**

> 안 팔리는 리스팅 10개는 0개와 같다. 중요한 건 개수가 아니라
> **경쟁이 덜한 키워드를 하나 더 갖는 것**이다.

그 기준으로 후보를 걸렀다:

| 후보 | 판단 |
|---|---|
| 단품 Habit 트래커 $7~9 | ❌ `habit tracker` 는 `adhd planner` 보다 **더** 붐비는 키워드다. 무료 대체재가 널렸고, 우리 강점(502p·탭 네비게이션·ADHD 특화 도구)이 작은 트래커에서는 전부 사라진다. v8에서 페이지를 떼어낸 형태라 중복 리스팅 리스크도 있다 |
| **ADHD 학생용 플래너** | ✅ **채택.** `adhd student planner` 라는 별도 키워드. 카테고리 1위 FutureADHD 가 이 상품을 갖고 있지 않다(확인함). 진짜로 다른 페이지 구성이라 중복 리스크 없음. 파이프라인 그대로 재사용 |
| 웨딩 플래너 | ❌ 파이프라인은 되지만 인구층·디자인 언어·신뢰도가 전부 다르다. 거대하고 경쟁이 극심한 카테고리에 맨몸으로 들어가는 셈 |
| 다크모드 variant | ⏸ 보류. FutureADHD 가 Dark/Sunset 병행 판매 중인 선례는 있으나, **Etsy 중복 탐지 임계값을 확인하지 못했다** |
| Notion 템플릿 | ❌ 파이프라인 불일치. 우리는 그 포맷을 만들지 못한다 |

### 조사의 한계 (알고 시작할 것)

Etsy 가 크롤링을 **403 으로 차단**해서 경쟁 리스팅의 실제 판매 수·리뷰 수를
가져오지 못했다. 검색 스니펫 수준의 근거다. **"수요가 있다"는 확인된 사실이
아니라 추정이다.**

확인된 것은 두 가지뿐:
- FutureADHD 숍에 학생용 플래너가 없다
- 디지털 번들이 "6종 $30어치를 $18" 식으로 통용된다

---

## 타겟

**ADHD 가 있는 대학생/대학원생.** 고등학생이 아니다 — 구매 결정권과 결제
수단을 본인이 갖고 있고, 영어권에서 "ADHD student" 검색의 주류다.

이 타겟의 실제 고통 (상품 설계의 축):

1. **강의계획서(syllabus)를 받고 아무것도 옮겨 적지 않는다** → 학기 중반에
   과제가 한꺼번에 터진다
2. **마감을 역산하지 못한다** → 전날 밤에 시작
3. **시험 공부 계획을 세우지 못한다** → 범위를 못 쪼갬
4. **조별과제에서 자기 몫이 뭔지 흐려진다**
5. 시간 감각 부재 — 과제에 걸릴 시간을 항상 과소평가

---

## 구성안

### v8 에서 그대로 재사용 (약 40%)

Brain dump / Focus session / Obstacle plan / Stuck on deciding /
Why I am avoiding it / Guess vs actual / **Working backwards** /
Energy budget / Medication log / Sleep / Mood / Notes(dot grid)

`Working backwards` 와 `Guess vs actual` 은 **학생 타겟에서 오히려 더
강력하다.** 마감 역산과 시간 과소평가가 이 타겟의 1·2번 고통이다.

### 새로 만들 것

| 페이지 | 해결하는 문제 |
|---|---|
| **Syllabus unpack** | 강의계획서 한 장을 과제·시험·읽기로 분해. 이 상품의 **핵심 셀링 포인트** |
| Semester at a glance | 학기 전체를 한 장에 |
| Class schedule | 주간 시간표 격자 |
| Per-class page | 과목별 정보·교수·오피스아워·성적 비중 |
| Assignment tracker | 과제 목록 + 마감 + 제출 여부 |
| Exam study plan | 범위를 쪼개고 날짜에 배분 |
| Lecture notes | 코넬 노트 형식 |
| Reading log | 읽을 것 / 읽은 것 |
| Group project | 누가 무엇을, 언제까지 |
| Questions for office hours | 물어볼 걸 미리 적어두기 |
| Grade tracker | 과목별 점수 누적 |
| Study session log | 언제 어디서 얼마나 집중됐나 |

### 반복 페이지

→ **아래 "확정 사항" 참조.** 8학기 / 주간 128 / 일간 248 로 확정했다.

**v8 의 "매일 한 장" 방식은 따라가지 않는다.** 학기당 112일을 다 넣으면
4학기만으로도 22MB 라 Etsy 상한을 넘는다. 학기당 31장으로 가고, 학생은
날짜가 아니라 **과목·과제 단위**로 쓰기 때문에 이게 실사용에도 맞다.

---

## 가격

| 항목 | 값 |
|---|---|
| 정가 | **$14.99** (추정, 확정 전) |
| 런칭 세일 | 40% → 약 $8.99 |

상품 1($16.99)보다 낮게 잡는 이유 — 페이지 수가 절반이고, 학생 타겟은
가격 민감도가 높다. **다만 $7~9 대 저가로는 가지 않는다.** 고유 페이지
45~55종은 그 가격대의 상품이 아니고, 첫 상품과의 가격 일관성도 깨진다.

---

## 제작 순서

1. `CLAUDE.md` 의 파이프라인 그대로. `PLANNER_VERSION=v9-student`
2. **기존 버전은 지우지 않는다.** v8 까지 전부 보존
3. 빌드 후 `python scripts/verify_v8.py` 를 v9 용으로 고쳐서 돌린다
   (페이지 수·링크·탭 하이라이트·월이름·연도·한글·용량 13개 항목)
4. `design-critic` 에이전트로 시각 검수
5. 목업 9장 → `scripts/build_mockups.py` 를 v9 용으로
6. 리스팅 원고 → `product2-listing.md`

**예상 소요: 2~3일**

---

## 미리 박아둘 함정 (v8 에서 전부 겪은 것)

- 제목 **140자 이내 + 전부 대문자인 단어 3개 이하**
- 설명문은 **문단 안에서 줄바꿈하지 않는다** (Etsy 가 그대로 살린 뒤 재접음)
- Description 칸과 Tags 칸을 헷갈리지 말 것
- 리스팅 폼에서 **Type = Digital** 을 먼저 잡아야 파일 업로드 칸이 나온다
- 빌드 후 반드시 **바이트 수 확인** (20MB 상한)
- undated 를 주장하면 **월 이름 검사**를 돌릴 것

---

## 확정 사항 (2026-09-21)

### 규모 — 8학기(4년) / 434페이지 / 약 17MB

페이지당 38.0KB(v8 실측)로 계산했다. **안전선 18MB 기준 최대 462페이지.**

| 세트 | 수량 |
|---|---|
| 고유 템플릿 | 약 50 |
| 학기 개요 | 8 |
| 주간 (학기당 16주) | 128 |
| 일간 (학기당 31장) | 248 |
| **합계** | **434p / 16.9MB** |

"Covers a full four-year degree" 가 셀링 포인트가 된다.

> **매일치 일간(학기당 112장)은 불가능하다.** 4학기만 해도 566페이지 22.0MB 로
> Etsy 상한 20MB 를 넘어 업로드 자체가 막힌다. 학기당 31장으로 간다.
>
> 이건 타협이 아니라 오히려 정직한 설계다 — 16주는 112일인데 ADHD 학생이
> 매일 일기를 쓰지 않는다. **"필요한 날에 쓰는 31장"** 으로 포지셔닝한다.

> ⚠️ 16.9MB 는 20MB 대비 여유 15% 다. v8 에서 4% 여유로 조마조마했던 걸
> 기억할 것. **빌드 후 반드시 바이트 수를 확인**하고, 넘치면 6학기(338p /
> 13.2MB)로 줄인다.

### 테마 — 하늘 워시 유지, 배경은 쿨 톤

v8 의 따뜻한 크림(`#FBF8F3`)과 구분되는 **쿨 페이퍼**로 간다. 하늘 사진
번짐은 그대로 쓰므로 숍 통일감은 유지되고, 배경만으로 별개 상품으로 읽힌다.

| | v8 (따뜻함) | **v9 (쿨)** |
|---|---|---|
| bg | `#FBF8F3` | `#F4F7FA` |
| field | `#F6F2EA` | `#EDF2F8` |

**액센트 4색은 v8 과 같이 간다** — 숍 전체가 한 브랜드로 보이게. 새 field
배경 위 대비를 전부 재측정했고 통과했다:

| 액센트 | 새 field 위 대비 |
|---|---|
| 블루 `#3E6E93` | 4.84:1 |
| 러스트 `#AC5038` | 4.71:1 |
| 그린 `#4A7248` | 4.92:1 |
| 머스터드 `#8A6415` | 4.77:1 |

전부 4.5:1 통과(7.5pt 작은 글자 기준). 후보였던 민트·라벤더 배경도 통과했으나
쿨 페이퍼가 대비가 가장 좋다.

---

## 새 세션을 위한 인계 (대화 없이 이것만 보고 시작할 수 있게)

이 절은 **우리 대화를 모르는 세션**이 바로 작업에 들어갈 수 있도록 코드 수준
으로 적었다. 위의 기획 내용과 `CLAUDE.md` 를 먼저 읽고 오라.

### 1차 — 테마 추가

`scripts/build_planner.py` 의 `THEMES` 딕셔너리에 아래를 그대로 추가한다.
`"v8-undated"` 항목 **바로 뒤**가 자연스럽다.

```python
    # 상품 2. v8 의 하늘 워시는 그대로 쓰되 배경을 쿨 톤으로 바꿔
    # 별개 상품으로 읽히게 한다. 액센트 4색은 v8 과 동일 -- 숍 전체가
    # 한 브랜드로 보여야 한다. 새 field 위 대비는 전부 재측정했고
    # 최저 4.71:1 로 통과했다(측정값은 이 문서 "테마" 절).
    "v9-student": {
        "font": "Nunito", "weights": "400;600;700;800", "display": None,
        "radius": "14pt", "tab_dots": True,
        "bg": "#F4F7FA", "card": "#FFFFFF", "ink": "#333A42",
        "mid": "#64707C", "soft": "#7C8792", "line": "#E2E8EF",
        "field": "#EDF2F8",
        "photo": "../assets/sky.jpg",
        "photo_cover": 0.14, "photo_page": 0.0,
        "bloom_cover": 0.42, "bloom_page": 0.55, "bloom_lift": 0.38,
        "bloom_cool": [
            (-104, -14, 138, 100, "#AFC9DF", .34),
            (-152, -22, 158, 108, "#A3C4E2", .30),
            (-206, -30, 172, 112, "#B6D2EC", .22),
        ],
        "undated": True,
        "student": True,
        "sections": {
            "index":    ("#7FA8C9", "#EAF2F8", "#3E6E93"),
            "semester": ("#7FA8C9", "#EAF2F8", "#3E6E93"),
            "week":     ("#7FA8C9", "#EAF2F8", "#3E6E93"),
            "day":      ("#7FA8C9", "#EAF2F8", "#3E6E93"),
            "classes":  ("#E08A73", "#FBEDE8", "#AC5038"),
            "work":     ("#E08A73", "#FBEDE8", "#AC5038"),
            "study":    ("#7FA37C", "#ECF3EB", "#4A7248"),
            "focus":    ("#7FA37C", "#ECF3EB", "#4A7248"),
            "life":     ("#D9A441", "#FBF1DC", "#8A6415"),
            "notes":    ("#D9A441", "#FBF1DC", "#8A6415"),
        },
    },
```

빌드해서 색감만 먼저 눈으로 본다. 이 단계에서는 페이지 구성이 v8 과 같아도
된다 — 목적은 쿨 배경 위에서 하늘 번짐과 액센트가 어떻게 보이는지 확인하는 것.

```bash
PLANNER_VERSION=v9-student python scripts/build_planner.py
```

### 2차 — 탭과 그룹을 학생용으로

`TABS` 와 `GROUPS` 는 현재 v8 전용으로 하드코딩돼 있다. `T.get("student")`
로 분기하거나 별도 상수를 둔다. **탭은 10개를 유지한다**(v8 과 같은 리듬).

| 탭 | 하위 페이지 |
|---|---|
| `INDEX` | 목차 |
| `SEMESTER` | 학기 개요 8장 |
| `WEEK` | 주간 128장 |
| `DAY` | 일간 248장 |
| `CLASSES` | 시간표, 과목별 페이지, 오피스아워 질문 |
| `WORK` | **Syllabus unpack**, 과제 트래커, 마감 역산, 조별과제, 시간 추정 |
| `STUDY` | 시험 계획, 코넬 노트, 읽기 기록, 학습 세션, 성적 트래커 |
| `FOCUS` | v8 재사용 |
| `LIFE` | 복약, 수면, 에너지 |
| `NOTES` | 점자 노트 |

**v8 에서 그대로 가져올 페이지 함수** (이름 바꾸지 말 것):

| 페이지 | 함수 |
|---|---|
| Brain dump | `p_braindump` |
| Focus session | `p_session` |
| Obstacle plan | `p_obstacle` |
| Stuck on deciding | `p_paralysis` |
| Why I am avoiding it | `p_avoiding` |
| Guess vs actual | `p_estimate` |
| **Working backwards** | `p_deadline` |
| Energy budget | `p_energy` |
| Medication log | `p_meds` |
| Sleep | `p_sleep` |
| Notes (dot grid) | `p_notes` |

`p_deadline`(마감 역산)과 `p_estimate`(시간 추정)는 **학생 타겟에서 오히려
더 강력하다** — 이 타겟의 1·2번 고통이 정확히 그것이다.

### 3차 — 반복 세트

v8 의 `build_html()` 안에 있는 `if T.get("undated"):` 블록이 본보기다.
같은 패턴으로:

```
s1..s8            학기 개요 8
s{n}-w1..w16      주간 128  (학기당 16주)
s{n}-d1..d31      일간 248  (학기당 31장)
```

`rail_key()` 에 새 정규식을 추가해야 한다. **v8 에서 이걸 빠뜨려 48페이지가
탭 하이라이트 없이 나간 이력이 있다**(`CLAUDE.md` 500페이지 절 5번).

### 검수

`scripts/verify_v8.py` 를 복사해 `verify_v9.py` 로 만들고 기대값을 고친다.
13개 항목(페이지 수·용량·링크·죽은 앵커·도달 불가·탭 10개·탭 하이라이트·
월이름 0·연도 0·한글 0)은 그대로 쓴다.

**용량을 반드시 확인한다.** 434페이지 예상이고 20MB 상한에 여유가 15%뿐이다.
넘치면 6학기(338p / 13.2MB)로 줄인다.

```bash
python scripts/dedupe_pdf.py output/planner_v9-student.pdf output/planner_v9-student-FINAL.pdf
python scripts/verify_v9.py
```

시각 검수는 `design-critic` 에이전트에 맡긴다. 상품 1에서 그림자 이음매와
대비 미달을 잡아낸 실적이 있다.

---

## 1차 결과 (2026-09-21)

`scripts/build_planner.py` 의 `THEMES` 에 `v9-student` 를 기획안 그대로 추가했다.
페이지 구성은 아직 v8 그대로(494p) — 목적이 색감 확인이었다.

`section_colors()` 에 분기를 하나 추가해야 했다. v9 의 `sections` 키가
학생용 탭 이름(`work`/`study`/`classes`...)이라 v8 의 `BUCKET_KEY`
(`tasks`/`meds`)로는 KeyError 가 난다. `STUDENT_BUCKET_KEY` 를 두고
`T.get("student")` 로 갈랐다. **색값은 v8 과 완전히 동일하고 키만 다르다.**

### 측정값 — 전부 통과

| 항목 | 결과 |
|---|---|
| 대비 (액센트 4색 × field `#EDF2F8`) | 4.71 ~ 4.92:1 — 기획안 수치와 일치 |
| 그림자 이음매 | 없음. 카드 하단 프로파일이 v8 과 사실상 동일 |
| 그림자 중앙 집중 | 중앙 peak 214, 좌우 223/219 — 의도대로 |
| 그림자 농도 | 중앙 31단계 (v8 은 33) — 쿨 배경이 약간 어두워 오히려 얕아짐 |

### 용량 — 기획 가정이 맞았다

dedupe 후 **18.33MB / 494p = 페이지당 38.0KB.** 기획안이 쓴 38.0KB 와 정확히
같다. **434페이지 환산 시 16.1MB** 로 추정치(16.9MB)보다 오히려 여유가 있다.
8학기 계획을 그대로 밀어도 된다.

### 남은 판단 하나 — `soft` 대비

`soft` 를 쿨 톤(`#7C8792`)으로 바꾸면서 흰 카드 위 대비가 **4.09:1 → 3.66:1**
로 내려갔다. 이 색은 7.5~8pt 작은 라벨(Meds/Water, 표 헤더, 힌트 문구)에 쓰인다.
v8 도 이미 4.5:1 미달이었으니 **새로 생긴 결함은 아니지만 더 나빠졌다.**

→ `#6D7883` 으로 바꾸면 정확히 4.5:1 이다(색상·채도 그대로, 명도만 낮춤).
**아직 반영하지 않았다.** 2차에서 결정한다.

## 디자인 컨셉 탐색 — 보류 (2026-09-21)

1차를 끝낸 뒤 "선과 면으로만, 동양화 느낌이지만 모던하게"라는 방향으로
두 단계를 시안까지 만들어봤다. **결론은 보류.** 확정 테마는 `v9-student`
(쿨 페이퍼 + 하늘 워시) 그대로다.

| 시안 | 무엇이었나 | 결과 |
|---|---|---|
| `v10-ink` (먹선) | 둥근 모서리 0, Playfair 제목 + 붓선, 활성 탭을 먹 면으로, 전통 안료 4색 | **코드에 남아 있다.** 보류 |
| 수묵 (여백) | 페이지 템플릿을 새로 짬. 카드 없음, 산수 커버, 여백이 주인공 | **폐기.** 스크립트는 남기지 않았다 |

### 왜 접었나

**1. 여백이 실행기능장애와 정면으로 싸운다. (제일 센 이유)**

ADHD 의 핵심 증상이 "시작을 못 한다"인데, **빈 페이지는 또 하나의 결정
부담**이다. 뭘 쓸지 스스로 정해야 하기 때문이다. 상품 1 이 강점으로 내세운
페이지들이 전부 정반대 성격인 것이 그 증거다:

> **Why I am avoiding it** — 회피 이유 **6지선다, 해당되는 것 체크**
> **Stuck on deciding** — "최선을 고를 필요 없다, 하나만 고르면 된다"

이건 비워주는 게 아니라 **떠먹여 주는** 디자인이다. 칸·라벨·선택지가 곧
상품 가치다. 수묵은 그 비계(scaffolding)를 걷어내는 언어라 **예뻐질수록
상품이 약해진다.** 거기에 단색이라 4색 코딩도 사라지는데, 그건 `CLAUDE.md`
가 조사 근거로 붙들고 있는 기억 보조 장치다.

**→ 이 논거는 수묵에만 적용되는 게 아니다.** 앞으로 어떤 시안이든
"칸을 줄이고 비운다"는 방향이면 같은 이유로 걸러야 한다.

**2. 타겟 취향 — 근거는 약하다.** 영어권 학생이 알록달록을 고른다는 판단
(사용자). **다만 이것만으로 접지 말 것.** Gen Z 학생 시장은 오히려
뉴트럴·미니멀이 강세다(studygram, 베이지 톤 노션 템플릿). 이 사유를 근거로
"더 알록달록하게"로 과교정하면 `listing.md` 가 명시적으로 피한 방향
("시끄러운 플래너는 안 열게 된다")으로 간다.

**3. 수묵은 격자에서 구조적으로 무너진다. (실증됨)** 시험 삼아 시간표를 찍어보니
세로선을 빼면 어느 요일 칸인지 알 수 없고, 넣으면 격자가 화면을 장악해
그 언어가 사라진다. 빠져나갈 구멍이 없다. **상품 2의 핵심 페이지는 전부
격자다** — 시간표·성적 트래커·과제 트래커·시험 계획. 즉 그 언어로는 상품의
절반이 만들어지지 않는다. (학기 16주 같은 **리스트형은 잘 버텼다**.
되는 것과 안 되는 것의 경계가 리스트/격자다.)

**4. 여백은 썸네일에서 진다.** 경쟁 리스팅이 꽉 찬 스프레드를 자랑하는
그리드에서 빈 페이지는 "내용이 없다"로 읽힌다.

### 남겨둔 것

`v10-ink` 테마와 `INK_CSS` 는 `scripts/build_planner.py` 에 그대로 있다
(`CLAUDE.md` — 기존 버전은 지우지 않는다). 되살리려면 그 테마를 쓰면 된다.
이때 부산물로 들어간 두 가지는 **보류와 무관하게 유지**한다:

- 인라인 점 마크업을 `.dot` 클래스로 추출 (값 동일, 기존 출력 불변)
- `--display` CSS 변수 — 테마가 제목 서체를 따로 지정할 수 있다

### 다음 방을 위한 경고

**이 탐색을 다시 하지 말 것.** 같은 결론에 다시 도달하느라 시간을 쓴다.
디자인을 바꾸고 싶으면 위 3번 사유(썸네일·격자·타겟 취향)부터 반박할 것.

---

## 채도 결정 (2026-09-21) — 학생용만 올린다

`v9-student` 의 액센트 4색을 **상품 1 보다 선명하게** 바꿨다. 색상(hue)은
그대로라 숍은 한 브랜드로 남고, 매대에서만 덜 묻힌다.

> 이유는 "학생이 알록달록을 좋아해서"가 **아니다**(위 보류 사유 2번 참조).
> 경쟁 썸네일 사이에서 눈에 띄어야 하기 때문이다.

### 계산은 OKLCh 로 했다

HLS 채도로 같은 배율을 먹이면 **그린만 형광으로 튄다**(`#55D54A` 가 나왔다).
HLS 채도는 지각적이지 않다. OKLCh 로 재보니 원래 팔레트부터 채도가
**제각각**이었다 — 그래서 네 색이 한 세트로 안 보였던 것이다:

| | 기존 C | 새 C |
|---|---|---|
| 블루 | 0.066 | **0.135** |
| 러스트 | 0.111 | 0.135 |
| 그린 | 0.069 | **0.135** |
| 머스터드 | 0.130 | 0.135 |

### 최종값

| 섹션 | 장식 | 틴트(칩) | 글자 |
|---|---|---|---|
| 블루 (index/semester/week/day) | `#4BABF0` | `#E4F3FE` | `#026FAB` |
| 러스트 (classes/work) | `#EB8367` | `#FEECE5` | `#AD4F36` |
| 그린 (study/focus) | `#62AC5D` | `#E2F8DF` | `#30782F` |
| 머스터드 (life/notes) | `#DBA339` | `#FEF1D4` | `#8C6305` |

글자용은 **카드·field·bg·칩 네 배경 전부**에서 4.5:1 통과(최저 4.65).

> ⚠️ **칩 배경 위 대비를 빠뜨리지 말 것.** `.chip` 은 틴트 위에 글자색을
> 얹는다. 카드 위만 재고 넘어가면 칩에서만 미달이 난다.

용량은 그대로다 — 38.0KB/page, 434p 환산 16.1MB.

---

## 디자인 확정 (2026-09-22)

컨셉 탐색(수묵·먹선)을 접은 뒤 **앱 UI 언어**로 갔다. 사용자가 레퍼런스로
제시한 것은 채도 높은 메시 그라데이션 위에 흰 카드가 떠 있는 형태다.

### 구조

| 요소 | 값 |
|---|---|
| 표지 | 다크 오로라 메시 (#14103A 바탕) |
| | 왼쪽 보라 `#7C4DFF` / 오른쪽 핑크 `#F45D9B` / 왼아래 주황 `#FF8A3D` / 오른아래 청록 `#22D3EE` |
| 내지 배경 | **표지와 같은 blob 목록**을 흰쪽으로 섞어 밝힌 것 |
| 내지 구조 | 표지 위에 14px 여백 두고 얹힌 시트, 라운딩 20pt, 아래 그림자 |
| 섹션 | 유리 `rgba(255,255,255,.55)`, 테두리 없음, `box-shadow:0 1.5pt 9pt rgba(40,28,90,.16)` |

> **내지는 표지를 톤다운한 것이어야 한다.** 처음에는 blob 좌표와 색이 다른
> '새 그림'을 그렸다가 "왜 표지랑 다르냐"는 지적을 받았다. 같은 목록을 쓰고
> 밝기만 올리면 색상 차이가 2~8° 안에 들어온다(실측).

### 점선 구분선 — 확정값 (공용. 모든 페이지가 이것만 쓴다)

전부 **고정 단위**다. 비율(%)은 한 군데도 쓰지 않는다.

| 항목 | 값 |
|---|---|
| 색 | `#d3d8de` |
| 선분 / 틈 / **주기** | 4pt / 4pt / **8pt** |
| 두께 | `1px` (= 0.75pt) |
| **줄 간격(행 높이)** | **24pt** |
| 양 끝 페이드 | 주기의 3배 = **24pt** |
| 면 가장자리 여백 | 가로 8pt, 세로 2pt |
| 열 너비 | 본문 폭에서 비율로 계산 (손으로 적지 않는다) |

> **주기와 줄 간격은 반드시 "4의 배수 pt".** 이게 이 항목의 핵심이다.
>
> 뷰어는 `pt × 배율`을 픽셀 격자에 맞춰 자른다. 처음 쓰던 6.75pt 주기는
> 어떤 흔한 배율에서도 정수가 안 나온다(×1.5=10.125, ×2=13.5, ×2.5=16.875,
> ×3=20.25). 그래서 **파일 안의 점은 전부 같은 길이인데**(24배로 재면
> 전부 71px) 화면에서는 4px·5px 로 갈린다. 줄 간격 27pt 도 ×1.5=40.5 라
> 줄마다 두께가 1px/2px 로 달랐다. 사용자가 본 "점선이 짧았다 길었다
> 굵었다 얇았다"가 전부 이 하나의 원인이다.
>
> 4의 배수면 0.25 단위 배율에서 항상 정수다. 실측 대조:
>
> | | 1.5배 | 2배 | 2.5배 | 3배 |
> |---|---|---|---|---|
> | 전 — 점 길이 | 4~5 | 5~6 | 7~8 | 8~9 |
> | 전 — 두께 | 1~2 | 2 | 2 | 3 |
> | **후 — 점 길이** | **6** | **8** | **10** | **12** |
> | **후 — 두께** | **2** | **2** | **2** | **3** |
>
> 줄 간격 28pt·32pt, 두께 1pt 도 같이 쟀지만 네 배율 모두에서 단일값이
> 나온 건 **24pt + 0.75pt** 뿐이었다. 값을 바꿀 일이 생기면
> `scratchpad/dash2.py` 로 네 배율을 다시 재고 나서 바꿀 것.

**`dline()` 함수 하나가 세 종류(세로 구분선·가로 구분선·필기 괘선)를 전부
만든다.** 손으로 베껴 쓰지 말 것 — 세 군데에 따로 써놨다가 시작점과 두께가
제각각이 됐다. 열 너비·괘선 길이·페이드는 전부 `CONTENT_L` 하나에서
파생된다. 레이아웃을 옮기면 자동으로 따라온다.

---

## 브라우저 인쇄 경로의 함정 (2026-09-22, 전부 실측)

화면에서는 되는데 **Chrome print-to-PDF 에서만 깨지는 것들**이다.
이걸 모르면 같은 하루를 다시 쓴다.

**1. `calc(100% - Npt)` 가 마스크 정지점에서 무시된다.**
`mask-image:linear-gradient(to right,transparent,#000 11pt,#000 calc(100% - 11pt),transparent)`
→ 왼쪽만 페이드되고 오른쪽은 뚝 끊긴다. 선 길이를 알고 있으므로 끝 좌표를
pt 로 직접 박아야 한다.

**2. `mask-composite: intersect` 가 안 먹는다.**
양쪽에서 마스크를 하나씩 걸어 교집합을 취하려 했더니 **마지막 레이어만**
적용돼 이번엔 오른쪽만 페이드됐다.

**3. `%` 단위는 요소마다 다른 결과를 낸다.**
마스크의 `10%` 는 *그 요소 자신의 길이*에 대한 비율이다. 칸 폭이
220pt / 60pt 로 다르면 페이드도 22pt / 6pt 로 달라진다. 세로선은 칸 높이가
27pt 뿐이라 10% = 2.7pt 로 아예 안 보였다.

**4. 페이드는 점 주기보다 충분히 길어야 한다.**
3.75pt 로 뒀을 때 점 주기보다 짧아 점 하나도 못 덮었고 "페이드가 없다"로
보였다. 점 3개 분량(= 주기 × 3)부터 읽힌다. 그래서 `FADE = 3 * PERIOD` 로
묶어 뒀다 — 주기를 바꾸면 페이드가 따라온다.

**5. `flex:1` 로 줄 높이를 나누면 두께가 들쭉날쭉해진다.**
남은 높이를 나눠 27.36px 같은 소수 높이가 나오고, 줄마다 y 소수점이 달라
렌더러가 다르게 반올림한다. 줄 높이는 **고정 pt** 로 박는다.

> 다만 **고정으로 박는 것만으로는 부족하다.** 27pt 로 박아도 뷰어 배율이
> 1.5배면 40.5px 이 되어 줄마다 갈린다. 위의 "4의 배수" 규칙까지 지켜야
> 끝난다. 파일 안 기하는 완벽한데 화면만 깨지는 경우가 있다는 뜻이고,
> 그래서 **검사는 정수 배율(12배)만으로는 통과시키면 안 된다** —
> 1.5·2·2.5·3배에서도 재야 한다.

**6. `nth-child` 규칙은 열 수가 다른 표마다 따로 만들어야 한다.**
4열 표에 맞춰 규칙을 만들었더니 6열 시간표의 THU·FRI 에 선이 안 그려졌다.

---

## 용량 실측 (2026-09-22) — 가장 중요한 제약

페이지를 2장/20장으로 뽑아 **한계 증가분**을 재고 434페이지로 환산했다.
Etsy 상한은 **20MB** 다.

| 구성 | 페이지당 | 434p | dedupe 후 |
|---|---|---|---|
| 기본(유리·텍스트·공유 배경 PNG) | 30 KB | 14.5 MB | 10.3 MB |
| + 섹션 `box-shadow` | +39 KB | | |
| + 파선(칸마다) | +164 KB | | |
| + `mask` 페이드 | **+331 KB** | | |
| **전부 합친 현재 조합** | **564 KB** | **240 MB** | 171 MB |
| 파선을 줄 단위로(요소 1/9) | 93 KB | 41 MB | 29 MB |
| 유리 + 실선 헤어라인 | 36.5 KB | 17.1 MB | **12.1 MB** |

**핵심: 용량은 점 개수가 아니라 요소 개수에 비례한다.**
파선 주기를 9px→30px 로 늘려도 233KB 로 동일했다. Chrome 이 요소마다
타일링 패턴 객체를 하나씩 박기 때문이다. 표 하나에 칸이 66개면 패턴이 132개.

**`mask` 가 압도적으로 비싸다**(요소당 2.5~4.5KB). 배경 PNG 는 전 페이지가
공유하므로 페이지당 비용이 0 에 수렴한다 — 이건 문제가 아니다.

> ⚠️ **현재 조합으로는 434페이지를 만들 수 없다.** 2차에서 실제 페이지
> 구성이 나온 뒤 다시 측정하되, 줄여야 한다면 순서는
> `mask` → 파선 → `box-shadow` 다. 지금 수치는 "표가 가득한 페이지가
> 434번 반복된다"는 가정이라 실제로는 더 낮을 것이다.

---

## 검사 자동화

`scratchpad/verify_lines.py` 가 빌드 끝에 자동으로 돈다. **실패하면
`SystemExit` 으로 빌드를 죽인다** — 통과 못 하면 렌더도 안 되고 보여줄
수도 없다.

검사 항목: 두께 편차 / 간격 편차 / 페이드 유무 / 좌우 대칭.
오늘 나온 결함 여섯 종류가 전부 이 넷 중 하나에 걸린다.

> **페이지 종류를 추가하면 `DEFAULT` 목록에도 반드시 넣을 것.**
> 434페이지라도 고유 템플릿 약 50종만 검사하면 반복 페이지는 자동으로 덮인다.
> 사람이 눈으로 검수하는 절차로는 오늘 같은 일이 반복된다.

---

## 제작 배치

v8 도 3차 배치로 만들었다. 같은 방식으로 간다.

- [x] **1차** — v9 테마 추가, 빌드해서 색감 확인 (2026-09-21 완료)
- [ ] **2차** — 학생용 고유 페이지 (Syllabus unpack, 시간표, 과제 트래커,
      시험 계획, 코넬 노트, 조별과제, 성적 트래커 등)
- [ ] **3차** — 반복 세트(학기 8 / 주간 128 / 일간 248) + 네비게이션
- [ ] **검수** — `verify` 스크립트 v9 용, `design-critic` 시각 검수
- [ ] **리스팅** — 목업 9장, 원고 `product2-listing.md`


---

## v0.1 빌드 (2026-09-22)

```bash
PLANNER_VERSION=student-v0.1 python scripts/build_planner.py
python scripts/verify_student.py
```

**버전 체계.** 상품 1의 시안 번호(v1~v8)를 이어받지 않는다. 별개 제품이므로
제품 기준으로 센다: 개발 중 **v0.x** → 출시 **v1.0** → 이후 수정 **v1.1**.
`v9-student` 는 이 이름을 정하기 전에 쓰던 키라 별칭으로만 남겨 뒀다.

| | |
|---|---|
| 페이지 | 428 (고유 33종 + 학기 8 + 주간 128 + 일간 248) |
| 용량 | 19.71 MB — **상한 20MB 대비 여유 1.4%** |
| 파일 | `output/planner_student-v0.1.pdf` |

> **용량 결정(2026-09-22):** 여유가 얇지만 **일단 올려 본다.** v8 이
> 19.23MB 로 통과한 전례가 있다. 업로드가 막히면 일간을 학기당 31→27장
> (428→396p, 약 18.3MB)으로 줄인다. 페이지를 먼저 줄이지 않는다.

### 파일 구조

| 파일 | 담는 것 |
|---|---|
| `scripts/app_style.py` | 레이아웃 상수·점선 생성기·유리 CSS·배경 굽기. **숫자가 사는 유일한 곳** |
| `scripts/student_pages.py` | 학생용 탭 구성과 페이지들 |
| `scripts/build_planner.py` | 테마에 `"app": True` 면 위 둘을 끌어다 쓴다 |
| `scripts/verify_student.py` | 산출물 검사 16항목 |

**v1~v8 은 이 경로를 타지 않는다.** 반영 후 v8·v3 의 생성 HTML 을 해시로
대조해 변경 전과 완전히 동일함을 확인했다.

### 용량을 170MB 에서 19.7MB 로 (전부 428페이지 실측)

| 바꾼 것 | 효과 |
|---|---|
| 점선을 CSS 의사요소 → **SVG 한 장** | 표 하나에 타일 패턴 객체 40~50개가 생기던 것이 몇 개로 |
| 괘선을 data URI 타일 → **인라인 SVG `<pattern>`** | 156 → 46 KB/page. `background-image` 는 요소마다 래스터화된다 |
| 배경을 큰 PNG 한 장 → **저해상도 3장 + CSS 로 자르기** | 38.4 → 7.5 KB/page. 내지 모서리는 이미지에 굽지 않고 CSS `border-radius` 로 자른다 |
| 유리면 `box-shadow` → `::after` 타원 | **`box-shadow` 는 +19.3MB**. 흐림을 절반으로 줄여도 2MB 밖에 안 빠진다 — 크기가 아니라 요소 개수가 비용이다 |
| 필기 괘선의 양 끝 페이드 포기 | SVG `mask` 하나가 4.1MB. 표의 페이드는 0.5MB 라 유지 |
| 탭 점·체크박스를 각지게 | `border-radius` 가 요소마다 호 4개. 둘이 합쳐 3.3MB |

> **사방 고르게 떠 있는 그림자는 20MB 안에서 불가능하다.** 대신
> **하단 중앙 그림자 + 윗변 0.7pt 흰 하이라이트**로 간다. 표지 카드가
> 쓰던 수법과 같고, 비용은 선 한 줄이라 0 이다.

### 세로 길이는 전부 12pt 의 배수

같은 페이지의 표끼리 점선 두께가 1px/2px 로 갈렸다. 원인은 면의 페이지상
y 가 표마다 0.25pt 씩 달랐던 것. 두 조건을 동시에 만족해야 한다:

- **3의 배수 pt** — Chrome 이 px 격자(1px = 0.75pt)에 눕히므로
- **4의 배수 pt** — 뷰어 배율 1.5/2/2.5/3배에서 정수 픽셀이 되도록

최소공배수가 **12pt(=16px)**. 그래서 `.content` 패딩 48/36, `.body` gap 24,
`.card` 아래 패딩 12, 라벨 높이 12 + 여백 12, 머리 높이 72, 표 머리띠 24,
줄 높이 24, 입력칸 높이 24/36 — 세로로 쌓이는 값이 전부 12의 배수다.
**새 페이지를 만들 때 이 격자를 깨지 말 것.**

### 남은 한 가지 (알고 넘어가는 것)

같은 페이지에서 **표의 점선과 필기면 괘선**이 2.5배 확대에서 1px 차이가
난다. 표끼리는 완전히 같다. 필기면은 면 높이가 유동적이라 SVG `<pattern>`
으로 깔 수밖에 없고, 뷰어가 패턴 행렬을 반올림하면서 생긴다.
`<path>` 로 직접 찍어 보았더니 **62MB** 가 되어 되돌렸다.
`verify_student.py` 가 계속 이 항목을 보고하므로, 더 나빠지면 바로 드러난다.

### 검사기 — `scripts/verify_student.py`

구조 13항목(페이지 수·용량·목적지·끊어진 링크·도달 불가 페이지·탭
하이라이트·월 이름·연도·한글 …)에 더해, 오늘 겪은 결함 3종을 항목으로 넣었다:

1. **점선 균일** — 1.5/2/2.5/3배에서 점 길이와 두께가 단일값인가.
   정수 배율(12배)로만 재면 전부 통과한다. 이걸로 두 번 속았다.
2. **면 그림자** — 유리면이 있는 고유 템플릿 전부에서 면 아래가 어두워지는가.
   `overflow:hidden` 이 `::after` 를 잘라 brain dump 만 그림자가 없었다.
3. **괘선 채움** — 마지막 줄이 면 바닥 가까이 오는가.
   `<svg>` 를 `width:auto` 로 두면 고유 크기(300x150px)로 누워 반쪽만 채운다.

> 검사기가 처음 돌자마자 **진짜 결함 3개**를 잡았다: 학기 개요 `t1~t8` 과
> 과목별 `c1~c8` 로 가는 **링크가 하나도 없었고**(스크롤 말고는 갈 방법이
> 없었다), CSS 주석의 **한글 716자**가 산출물에 실렸고, 탭 목차의
> "Anything else" 칸이 **빈 면**이었다(v8 용 `lines()` 를 쓰고 있었다).
>
> **검사가 사용자의 눈보다 느슨하면 검사가 아니다.** 새 결함을 만나면
> 고치기 전에 항목을 먼저 추가할 것.

### 측정할 때 두 번 빠진 함정

- 스캔 대역에 **세로 구분선이나 면 가장자리**가 끼면 3~4px 짜리 가짜 선이
  잡힌다. 가로로 점이 5개 이상 끊긴 줄만 세야 한다.
- 면 **사이 간격 한가운데**를 재면 그림자가 없어 보인다(코넬 노트에서
  깊이 8 이 나왔는데, 각 면의 중앙에서는 25 였다).


---

## 2026-09-23 인수인계 — 여기부터 이어서

**회사 PC 에서 작업하다 연휴로 중단. 집에서 pull 받아 이어간다.**
`output/` `src/` 는 gitignore 라 안 넘어온다. 아래 한 줄로 다시 만든다:

```bash
PLANNER_VERSION=student-v0.1 python scripts/build_planner.py
python scripts/verify_student.py
python scripts/check_lines.py src/planner_student-v0.1.html    # Prod 1 검사기
```

### 지금 상태 — **빌드는 되지만 상한을 넘었다** (→ 오해였다. 맨 아래 "집 PC 에서 이어서" 절)

| | 값 |
|---|---|
| 페이지 | 428 |
| 용량 | **21.21 MB** ← Etsy 상한 20MB 초과. **이것부터 고쳐야 한다** |
| 검사 | 16항목 중 13 통과 (용량 / 도달 불가 5 / 점선 균일 3) |

### 오늘 끝낸 것

1. **목차 링크** — 네 줄이 전부 `href="#index"` 라 눌러도 제자리였다.
   9개 탭 전부로 고쳤다(`build_planner.py` 의 `APP_GROUPS` 를 3원소로).
2. **표지 카피** — "Semester at a glance" 는 없는 페이지였다 → `Term at a glance`.
3. **흰 실선 제거 (두 군데)** — 면의 `::before` 하이라이트, 다크 카드의
   `border-top-color`. 둘 다 "빛 받는 윗면"을 의도했는데 실제로는 뭉툭하게
   끊긴 흰 줄이었다. 그라데이션으로 흐리는 시안은 20.11MB 라 못 썼다.
4. **표 마감선** — `LINES.md` 2 절대로 마지막 행 아래를 닫았다. 세로 구분선은
   경계마다 한 줄로 통합하고 대시 위상을 밀어 행 경계마다 틈이 오게 했다
   (가로선과 십자로 안 만난다).
5. **`border-style:dashed` 실험** — v8 방식을 재 봤더니 **27.58MB**.
   Chrome 이 점선 테두리를 작은 사각형으로 내보낸다. 실선일 때만 싸다.
   스위치(`DASH_BORDER=1`)는 남겨 뒀다.

### 진행 중 — 반복 세트 재배분 (**여기서 멈췄다**)

기획서가 "학생은 날짜가 아니라 과목·과제 단위로 쓴다"고 적어 놓고 페이지
예산의 88%(376/428)를 날짜에 썼다. 그래서 Syllabus unpack 이 1장이라
2과목째부터 대응이 없었고 시간표가 1장이라 2학기에 상품이 끝났다.

**재배분은 코드에 이미 들어갔다**(`student_pages.py` 의 `REPEATS` / `specs()`):

| 앞글자 | 세트 | 학기당 | 합 |
|---|---|---|---|
| t / o / r | 학기 개요 · 목표 · 회고 | 1 | 각 8 |
| w | 주간 | 16 | 128 |
| d | 일간 | **14** (전 31) | **112** (전 248) |
| h / k | 시간표 · 연락처 | 1 | 각 8 |
| c | 과목별 | **5** | **40** (전 8) |
| s | Syllabus unpack | **5** | **40** (전 1) |
| a | 과제 트래커 | 1 | 8 |
| e | 시험 계획 | **3** | **24** (전 1) |
| g | 성적 트래커 | 1 | 8 |

**제목의 번호 체계도 같이 고쳤다.** 전에는 목차 TERM 3 의 "4" 를 누르면
"Week 36" 이 나와 잘못 눌렀다고 판단하게 됐다. 이제 eyebrow 가 `Term 3`,
제목이 `Week 4` 다(`student_pages.head()`).

### 남은 문제 두 개 — 이어서 할 일 (**둘 다 해결.** 맨 아래 절)

**1. 용량 21.21MB → 20MB 아래로.** 표가 많은 페이지(syllabus·class·exam)가
일간보다 페이지당 두 배쯤 비싸서, 장수를 맞췄는데 용량이 늘었다.
다음 값을 줄여 재보는 것이 다음 수다(`student_pages.py` 상단 상수):

```
EXAMS_PER_TERM     3 -> 2    (-8장)
CLASSES_PER_TERM   5 -> 4    (-8장)
DAYS_PER_TERM     14 -> 12   (-16장)
```
표가 많은 페이지 8장 ≈ 0.7MB, 일간 16장 ≈ 0.7MB 로 어림잡았다.
**한 번에 하나씩 줄이고 매번 바이트를 잴 것.**

**2. 도달 불가 페이지 5개 (`h1`~`h5`).** HTML 에는 `href="#h1"` 이 있고
PDF 링크 주석에도 `/h1`~`/h8` 이 다 있는데, 검사기는 272~276 페이지가
어디서도 도달 불가라고 한다. `h6`~`h8` 은 정상이다. **명명 목적지 `/h1` 이
엉뚱한 페이지를 가리키고 있을 가능성**이 높다 — `<h1>` 태그와 이름이
겹치는 것과 관련이 있는지 먼저 볼 것. 겹침이 원인이면 앞글자를 `h` 말고
다른 글자로 바꾸면 끝난다.

### 그 다음 (어제 전수 검사 4건의 남은 항목)

| | 항목 |
|---|---|
| A | 문서 제목 메타가 아직 **"ADHD & Wellness Planner"**(상품 1 것) |
| B | **PDF 북마크 0개.** `pypdf` 로 40~50개. 바이트 비용 ~0, 효과 큼 |
| C | `syllabus`/`backwards`/`grades` 하단 잘림, `focus-session` 의 `.row` 에 `flex:none` 누락(222pt 공백) |
| D | 날짜 적는 칸이 428페이지 어디에도 없다 (undated 상품의 필수 부속) |
| E | 학기 개요 → 그 학기 주간으로 가는 링크 없음 |
| F | 리스팅 문구 3건이 방어 불가 — `Every page is one tap away`(거짓), `33 UNIQUE TEMPLATES`(실제 30 또는 40), `Not one page printed four hundred times`(실제 반복 비중이 더 높다) |
| G | 인쇄 주장 — 잉크 커버리지 9.2%(v8 은 2.7%), 괘선 대비 1.14:1. `or print it` 을 빼거나 printable 별도 파일 |
| H | 검사기에 추가할 3항목: 자기 페이지로 가는 링크 = 실패 / 마지막 섹션 라벨 아래 내지 ≥72pt / 링크처럼 생긴 행 개수 == 링크 주석 개수 |

---

## 2026-09-23 밤 — 집 PC 에서 이어서 (여기가 최신)

빌드 순서가 바뀌었다. **검사기는 이제 `-FINAL` 을 잰다.** dedupe 를
빼먹으면 검사기가 멈추고 명령을 알려준다.

```bash
PLANNER_VERSION=student-v0.1 python scripts/build_planner.py
python scripts/dedupe_pdf.py output/planner_student-v0.1.pdf output/planner_student-v0.1-FINAL.pdf
python scripts/verify_student.py
python scripts/check_lines.py src/planner_student-v0.1.html
```

| | 값 |
|---|---|
| 페이지 | 428 |
| 용량 (FINAL) | **14.88 MB** — 여유 25% |
| verify_student | 18항목 중 17 통과. 남은 것은 `d1` 점선 균일 3건(위 "알고 넘어가는 것") |
| check_lines | 넘침 0. "여유 0px 369개"는 **오탐** -- v8 의 div 괘선을 전제로 한 규칙이고, 학생용 SVG 패턴은 마지막 줄이 면 가장자리 4pt 위에 온전히 있다(4배 확대로 확인) |

### 고친 것

1. **용량 21.21MB 는 오해였다.** `verify_student.py` 가 dedupe **전** 파일을
   쟀다. 판매본(FINAL)은 14.94MB. 장수는 줄이지 않았다. 검사기가 FINAL 을
   재도록 바꾸고, FINAL 이 빌드보다 오래됐으면 거부한다(낡은 파일 측정 방지).
2. **`h1`~`h5` 도달 불가 — 원인은 SVG 그라데이션 id.** `app_style.rules_svg()`
   가 표마다 `id="h0"~"h5"` 를 붙여 시간표 페이지 id 와 겹쳤고, Chrome 이
   명명 목적지를 1페이지의 `<linearGradient>` 에 걸었다. 시간표 칩을 누르면
   표지로 갔다. id 를 표 모양의 crc32 로 만든다(`rg%08x`).
3. **덤으로 드러난 결함: 시간표 월·화·수 열에 가로 괘선이 없었다.** 같은
   id 라 `url(#h1)` 이 문서의 첫 표(4열) 정의로 풀려, 6열 표가 남의 좌표를
   빌려 썼고 좌표 밖이라 투명했다. 2번 수정으로 같이 고쳐졌다. 다른 표는
   픽셀 변화 0.
4. **Syllabus / Grade tracker / Working backwards 하단 잘림**(위 C 항목 일부).
   "Anything else" 라벨만 남고 면이 페이지 밖이었다. 셋 다 그 카드를 뺐다.
   Grade·Backwards 는 빈자리를 표 14→16행으로 채웠다.
5. **검사기의 `\b` 가 백스페이스 문자(0x08)로 저장돼 있었다.** "HTML 속 연도"
   검사는 한 번도 아무것도 못 잡는 상태였다. 고쳤고 지금 0건으로 통과.
   Claude 도구 입력에서 역슬래시가 풀리는 탓으로 보인다 -- 정규식을 파일에
   쓸 땐 `chr(92)` 를 쓰거나 저장 후 제어문자를 확인할 것.

### 추가한 검사 (`verify_student.py`)

| 항목 | 고치기 전 버전에서 |
|---|---|
| 다른 요소와 겹친 페이지 id | `h1`~`h5` 5건 걸림 |
| 정의가 여럿인 SVG id | 옛 `app_style` 로 표 3개 만들면 `h0`~`h3` 걸림 |

### 아직 남은 것

위 "그 다음" 표의 A·B·D·E·F·G·H 전부, 그리고 C 중 `focus-session` 의
`.row` `flex:none` 누락.

### 2026-09-24 — 맨 아래 선 정리 (집 PC, 사용자 검수 반영)

"표나 섹션 맨 아래 가로 점선이 애매하게 끝에 있다"는 지적.

| | 전 | 후 |
|---|---|---|
| 필기면 369곳 | 바닥 나머지 0~22pt, 마지막 괘선이 가장자리 근처에 뜸 | 24pt 배수로 고정, 맨 아래 괘선 없음 |
| 표 483곳 | 마감선이 둥근 가장자리에 겹침 | 마감선 없음. 가장자리가 닫는다 (LINES.md 2 절 예외) |
| Work·Study 목차 "Anything else" | 고정 후 1행(괘선 0개) | 칩 격자 행 간격 6->4.5pt 로 12pt 확보해 2행. Classes 목차도 6->7행 |

- `app_style.snap_lines()` 가 빌드 끝에 면을 재서 높이를 박는다.
  `build_planner.py` 에 앱 테마일 때만 호출하는 두 줄을 넣었다(Prod 1 영향 없음)
- Anything else 를 빼는 안도 렌더해 봤으나 아래가 크게 비어 기각(사용자 결정)
- `verify_student.py` 에 "24pt 배수가 아닌 필기면"(전 368), "표 가장자리의
  마감선"(전 483) 추가. `check_lines.py` 는 이제 실패 0
- 결과: 428p / **14.44MB** / verify 20항목 중 19 통과(`d1` 점선 균일 2, 알고 넘어감)

**낡은 PDF 통과 구멍 — 막았다.** 빌드가 PDF 를 못 썼는데 `빌드 | tail &&
dedupe` 의 파이프가 종료 코드를 삼켜 dedupe 가 낡은 PDF 로 새 FINAL 을
만들었고, 검사기는 FINAL 이 RAW 보다 새것인지만 봐서 통과시켰다. 이제
**RAW 가 HTML 보다 오래됐으면 거부**한다(빌드는 HTML 을 먼저 쓴다).
같은 상황을 재현해 옛 검사기는 통과, 새 검사기는 거부함을 확인했다.
명령을 파이프로 이을 때는 `set -o pipefail` 을 먼저 켤 것.

**A 완료 (2026-09-24).** PDF `/Title` 을 `ADHD Student Planner — Undated
Semester Planner` 로. `build_planner.py` 가 테마의 `student` 플래그로 가른다.
상품 1 값은 그대로 두어 판매본을 바이트까지 다시 뽑을 수 있다. 리스팅
제목(F)을 정하면 여기도 맞춘다. 검사 "문서 제목에 Student" 추가(전: 실패).

**B 완료 (2026-09-24).** PDF 북마크 133개. 맨 위는 탭 10개, 반복 세트는
학기별로 한 단계 더(Term 1~8), Weeks·Days 는 학기가 바로 탭 아래. 하위가
있는 항목은 접어 둔다. `student_pages.outline()` 이 TABS/GROUPS/REPEATS 에서
뽑으므로 페이지를 바꾸면 북마크가 따라간다. 빌드 끝에 pikepdf 로 심고
dedupe 를 거쳐도 남는다. 용량 영향 없음(14.43MB). 검사 4항목 추가: 북마크
수 / 맨 위 = 탭 순서 / 목적지 없는 북마크 / "Term N" 이 그 학기 페이지로 가는가.
Chrome 내장 뷰어의 사이드바는 Claude 가 클릭할 수 없어 실제 화면 캡처는 못 했다.
GoodNotes 에서 보이는지는 사용자 확인 필요.

**다음:** 위 C(focus-session)·D·E·F·G·H.
