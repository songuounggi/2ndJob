# 출시·교체 전 검수와 인계 — 모든 방 공통

**Etsy 에 파일을 올리거나 바꾸기 전에 이 문서를 처음부터 끝까지 따른다.**
상품 1·상품 2·앞으로의 상품 모두 같다.

2026-09-24 Prod 1 (Home) 방에서 판매 중인 상품에 결함 두 개가 나왔고, 고치고
전달하는 과정에서 실수가 여러 번 이어졌다. 전부 **사용자가 iPad 와 Etsy 화면을
직접 보다가** 찾았다. 이 문서는 그 하루를 다시 겪지 않기 위한 것이다.

---

## 0. 그날 무슨 일이 있었나 (요약)

| # | 일어난 일 | 누가 찾았나 | 원인 |
|---|---|---|---|
| 1 | 판매본(v8.18)을 iPad GoodNotes 에서 넘기면 페이지가 **바둑판처럼 조각조각** 채워졌다 | 사용자 iPad | 카드 그림자(CSS gradient)와 배경 번짐(opacity)이 전 페이지에. 페이지당 렌더 ~200ms |
| 2 | 도트 그리드 노트가 **4배 넓은 간격, 흐릿한 네모**로 나왔다 | 사용자 iPad | CSS 반복 배경이 PDF 에 **이미지 타일 패턴**으로 들어가고, GoodNotes 가 그 배율을 다르게 읽었다 |
| 3 | 첫 구매자 감사 메시지 초안에 **"탭이 오른쪽"** (실제는 왼쪽) | 사용자 | 파일을 안 보고 짐작으로 씀 |
| 4 | 안내문에 **iPad 언급을 뺐다**, 사용법을 일반 PDF 안내로만 채웠다 | 사용자 | 상품(undated, iPad, MONTH→달→날짜 흐름)을 공부하지 않음 |
| 5 | 확인 전인 새 판으로 **마켓 판 파일을 덮어썼다** | 사용자 | 파일 규칙 없이 움직임 |
| 6 | 폴더 규칙을 멋대로 두세 번 바꿨다(`upload/2/` 등) | 사용자 | 사용자와 정하지 않고 만듦 |
| 7 | 새 판을 **Etsy 메시지 첨부**로 보냈더니 받는 파일의 **링크 5,893개가 0개**, 파일명도 바뀜 | 사용자 | 첨부가 "된다"만 확인, 받는 쪽 결과를 확인 안 함 |
| 8 | "View files 크기를 보라"를 **왜 보는지 설명 없이** 반복 | 사용자 | 지시만 하고 목적을 안 말함 |
| 9 | Etsy 가 MB 를 1000/1024 로 화면마다 다르게 표시 → 예상 숫자가 틀림 | 사용자 | 한 화면만 보고 단정 |

**공통 원인: PC 에서 파일 구조만 보고, 구매자가 실제로 보는 것(iPad 화면, Etsy
화면, 받은 파일)을 확인하지 않았다.**

---

## 1. 출시·교체 전 검수 순서

전부 통과해야 Etsy 에 올린다. 하나라도 빠지면 올리지 않는다.

### 1-1. 자동 검사 (PC)

```bash
python scripts/verify_<버전>.py                          # 구조: 페이지·링크·탭·날짜 (상품별)
python scripts/check_lines.py src/planner_<버전>.html    # 선 일관성 (LINES.md)
python scripts/check_render.py output/planner_<버전>-FINAL.pdf   # 뷰어 위험·속도·용량
python scripts/check_upload.py                            # 업로드 이력표 ↔ 파일 (상품 1)
```

`check_render.py` 가 재는 것 (2026-09-24 신설):

| | 항목 | 기준 | 왜 |
|---|---|---|---|
| A-1 | 그라데이션 셰이딩 있는 페이지 | 0 (표지 제외) | GoodNotes 가 타일마다 계산 → 바둑판 |
| A-2 | 소프트마스크(반투명) 있는 페이지 | 0 (표지 제외) | 반투명 합성 → 느림 |
| A-3 | 이미지 타일 패턴 있는 페이지 | 0 (표지 제외) | GoodNotes 가 배율을 다르게 읽음 (도트 4배) |
| B | 페이지 렌더 시간 (scale 2.7) | 최대 < 150ms | v8.18 = ~200, v8.20 = ~65 |
| C | pdfium vs MuPDF 차이 | 경고만 | 엔진이 갈리는 페이지 신호. **도트 결함은 못 잡는다** |
| D | 용량 | < 20,000,000 B | Etsy 디지털 파일 상한 |

> **C 측정 방식 변경 (2026-09-24, Prod 2).** pdfium 쪽만 `convert('L')`(밝기
> 가중치), MuPDF 쪽은 RGB 평균이라 채도 높은 페이지에서 가짜 차이가 났다(상품 2
> 목차 14.2). 둘 다 RGB 평균으로 맞췄다. 바뀐 값: v8.20 최대 0.94, v8.18 1.23,
> student-v0.3 0.90 -- 셋 다 경고 없음. A·B·D 판정은 그대로.

기준값 실측 (집 PC):

| 파일 | A-1 | A-2 | A-3 | B 최대 | 결과 |
|---|---|---|---|---|---|
| v8.18 (옛 판매본) | 501 | 501 | 9 | 264ms | FAIL 4 |
| v8.19 | 0 | 0 | 3 | — | FAIL (도트) |
| **v8.20 (판매 중)** | 0 | 0 | 0 | 101ms | **통과** |
| student-v0.1 (09-24 11:49) | 427 | 427 | **357** | 542ms | **FAIL 4 -- 상품 2 가 고칠 것** |
| **student-v0.3** | 0 | 0 | 0 | 146ms | **통과** (v0.2 에서 수정, v0.3 에서 표 페이드 복원) |

### 1-2. 눈으로 보기 (PC 렌더)

`CLAUDE.md` "작업 규칙" 대로 PNG 로 렌더해서 본다. 바뀐 페이지는 전부,
반복 페이지는 표본으로.

### 1-3. iPad 실기기 체크리스트 — **사용자가 한다. 이게 통과해야 올린다**

Claude 는 iPad 를 만질 수 없다. 대신 **무엇을 어느 페이지에서 볼지 페이지
번호까지 뽑아서** 사용자에게 드린다. 파일은 GoodNotes 로 연다(구매자 대다수 환경).

| # | 할 일 | 볼 것 |
|---|---|---|
| 1 | GoodNotes 에서 파일 열기 | 오래 걸리지 않는가 |
| 2 | 첫 30페이지를 빠르게 넘기기 | 바둑판처럼 늦게 채워지지 않는가 |
| 3 | 왼쪽 탭 10개를 하나씩 누르기 | 전부 이동하는가, 현재 탭이 표시되는가 |
| 4 | MONTH → Month 1 → 날짜 → 위의 Month 칩 | 3탭 흐름과 돌아오기 |
| 5 | 도트·줄지·무지 노트 확대 | 흐리거나 간격이 이상하지 않은가 |
| 6 | 아무 페이지 확대, 카드 그림자와 선 | 이음매·흐림 |
| 7 | 펜으로 몇 줄 써 보기 | 필기가 자연스러운가 |

바뀐 페이지가 있으면 그 번호를 체크리스트에 덧붙여 드린다.

---

## 2. 스크롤·렌더 최적화 규칙 (HTML/CSS → Chrome PDF)

**PC 에서 멀쩡해도 iPad 에서 다를 수 있다.** 새 CSS 효과를 쓰면 PDF 에 무엇으로
들어갔는지 `check_render.py` 로 확인한다.

| 쓰지 않는다 | 대신 | 사례 |
|---|---|---|
| `box-shadow` | 0.4pt 헤어라인 + 그림자 PNG | 494p 에서 56MB (CLAUDE.md "500페이지") |
| 카드 그림자 `radial-gradient` | **공유 PNG 한 장**을 모든 카드가 늘려 씀 (`shadow_png()`) | v8.18 바둑판 → v8.20 |
| 배경 번짐을 `opacity` 로 겹치기 | **배경색에 미리 합성한 불투명 PNG** (`bloom_baked_png()`) | 같음 |
| 겹겹 CSS gradient 배경 | PNG 한 장을 전 페이지가 공유 | 2,973ms/page → 49ms |
| 반복 배경으로 도트·격자 (`radial-gradient` + `background-size`) | **진짜 벡터 도형** (`dot_svg()`: path 하나에 원 전부) | v8.19 도트 4배 |

- 켜는 법: 테마 플래그 `fast_paint=True`, `vector_dots=True` (`scripts/build_planner.py`)
- **효과: 렌더 ~3배 빠름, 용량 19.6 → 16.5MB.** 모양 차이 평균 0.086/255
- 표지 한 장은 사진 위 반투명이 남아 있다(예외로 둠)
- **상품 2 (`app_style.py`) 는 student-v0.2 부터 따른다** (플래그 `flat_paint`, `dash_fade`). `product2-student.md` 끝 절

---

## 3. 버저닝과 파일 규칙 (사용자 확정 2026-09-24)

### 3-1. 버전을 올려 가며 고친다

```python
# scripts/build_planner.py -- 새 줄을 추가한다. 옛 줄은 지우지 않는다
THEMES["v8.21-undated"] = dict(THEMES["v8-undated"], fast_paint=True, vector_dots=True, <새 플래그>=True)
```

- 고칠 때마다 **새 버전 이름**(v8.20 → v8.21). 같은 이름으로 다시 빌드해 덮어쓰지 않는다.
  사용자가 이미 받은 파일 이름과 같으면 어느 게 어느 판인지 헷갈린다
- 바뀐 동작은 **플래그**로 켠다. 그래야 옛 버전을 다시 빌드해도 옛 결과가 나온다
- `build_planner.py` 상단 버전 주석에 한 줄: `#   v8.21-undated 날짜  무엇을 왜`
- 검증 스크립트도 버전별로: `verify_v8_21.py` (직전 것을 복사하고, **이번 결함을
  재는 항목을 추가**. 고치기 전 버전에 돌려 FAIL 하는지 먼저 본다)
- 빌드 → dedupe → 1절 검수:

```bash
PLANNER_VERSION=v8.21-undated python scripts/build_planner.py
python scripts/dedupe_pdf.py output/planner_v8.21-undated.pdf output/planner_v8.21-undated-FINAL.pdf
```

### 3-2. 파일이 어디에 있는가

| 위치 | 담는 것 | 규칙 |
|---|---|---|
| `output/planner_<버전>-FINAL.pdf` | 작업본 | 버전마다 하나. **지우지 않는다** |
| `output/upload/<버전>/<Etsy 이름>.pdf` | **Etsy 에 실제로 올린 판** | 사용자 최종 확인 → 올린 뒤에 **버전 이름 폴더를 새로** 만든다. **한 번 넣은 파일은 건드리지 않는다** |
| `output/upload/` 바로 아래 | — | **파일을 두지 않는다** |

- 폴더 이름은 짧은 버전명: `upload/v8.18/`, `upload/v8.20/`. **숫자 폴더(2/, 3/) 쓰지 않는다**
- 가장 새 버전 폴더 = 지금 마켓 판
- Etsy 이름(상품 1): `ADHD-Wellness-Planner-Undated-502-pages.pdf`. 올린 뒤엔 못 고친다.
  70자 이내, 영숫자 `.` `_` `-` 만
- 무엇을 언제 올렸는지는 `shop.md` 0-1절 이력표. `check_upload.py` 가 대조한다
- `output/` 은 git 에 없다. 다른 PC 에서는 `SETUP.md` 로 다시 빌드한다

### 3-3. 규칙을 새로 만들지 않는다

폴더·파일 이름·순서를 바꿔야 할 것 같으면 **먼저 사용자에게 쉬운 말로 묻는다.**
작업 중에 새 체계를 만들면 사용자가 매번 다시 배워야 한다(6번 사례).

---

## 4. Etsy 에서 파일을 바꿀 때 — 확인된 사실

| 사실 | 근거 |
|---|---|
| 리스팅 파일을 바꿔도 **기존 구매자는 구매 당시 파일만** 받는다. Purchases 에서 다시 받아도 옛 파일 | 교체 직후 구매자 다운로드 화면 18.69MB 그대로 + 셀러 커뮤니티 |
| 구매자는 Purchases → Download 에서 **돈을 다시 내지 않고** 언제든 다시 받는다 | Etsy 공식 (`shop.md` 5-5) |
| **메시지에 PDF 를 첨부하면 Etsy 가 다시 저장하면서 링크를 전부 지운다.** 파일명도 `icm_fullxfull....pdf` 로 바뀐다 | 링크 5,893 → 0, Producer `pdfcpu` (2026-09-24 실측) |
| 이메일·외부 링크(Drive 등)로 보내는 것은 **Etsy 메시지 규정 위반** | Etsy Off-Platform Transactions 정책 |
| Etsy 는 화면마다 MB 를 다르게 쓴다: 리스팅 편집 = 1000 단위, 주문 `View files` = 1024 단위 | 16,517,551 B → `16.51 mb` / 19,599,557 B → `18.69 MB` |
| 보낸 메시지는 고치거나 지울 수 없다 | Etsy 화면 |

**결론: 기존 구매자에게 새 판을 링크째 줄 방법은 지금 없다.** 그래서 **올리기
전에** 1절 검수를 끝낸다. 올린 뒤에 고치면 이미 산 사람은 옛 판에 남는다.

---

## 5. 구매자에게 가는 글

1. **상품부터 공부한다.** `listing.md` 전문(제목·태그라인·HOW IT WORKS·WHAT YOU
   NEED)을 읽고, 판매본에서 링크 흐름을 직접 따라간다. 상품 1 의 핵심:
   - 날짜 없는(undated) 성인 ADHD 플래너. "Start any day. Skip a week. Nothing to catch up on."
   - 제목 "GoodNotes **iPad** Planner", 태그라인 "iPad & Android"
   - **왼쪽** 탭 10개. 특정 날짜 = MONTH → Month N → 날짜(3탭). 데일리의 Month 칩으로 돌아감
   - "Month 1" = 시작하는 달. 맨 위에 달 이름을 쓴다
2. **문장별 근거표**를 초안과 함께 낸다(`shop.md` 0-1절 규칙). 리스팅에 없는 약속은 새로 하지 않는다
3. **받는 쪽에서 어떻게 보이는지까지** 확인한다. 첨부가 있으면 받은 파일을 다시 열어 본다(7번 사례)
4. 톤: 사과문처럼 쓰지 않는다. 결함 수정도 "업데이트" 로. 정정은 한 줄
5. 외부 링크·이메일·다른 상품 홍보·쿠폰 넣지 않는다

---

## 6. 사용자에게 보고할 때

- **무엇을, 어디서, 왜** 보라는지 함께 말한다. "이 숫자를 알려주세요" 로 끝내지 않는다
  -- 그 숫자가 무엇을 가르는지까지 적는다(8번 사례)
- 사용자가 할 일은 **메뉴 이름·버튼 모양·파일 경로**까지 단계로 쓴다
- 내부 용어(이력 번호, destination, soft mask 등)는 쉬운 말로 바꾸거나 한 줄로 푼다
- 확인 못 한 것은 "미확인" 이라고 쓴다. 한 화면만 보고 단정하지 않는다(9번 사례)
- 수정 보고는 `CLAUDE.md` "작업이 끝나면" 형식: 파일 / 페이지 번호 / 볼 것 / 범위
