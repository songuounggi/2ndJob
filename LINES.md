# 선의 일관성 규칙

모든 버전 공통. **v8(판매 중), v9-student, 앞으로의 모든 상품**에 적용한다.

이 문서가 생긴 이유: 2026-09-23 에 구매자 눈으로 상품을 보다가 두 가지를
발견했다. 표의 첫 구분선이 한 칸에서만 빠져 있었고, 괘선 간격이 페이지마다
6.5배까지 차이가 났다. **둘 다 실수였고, 둘 다 눈으로만 검수했으면 또 놓쳤다.**

---

## 1. 괘선 (`.lines`) — 높이는 고정 pt 로 박는다

### 하지 말 것

```css
.lines > div { flex: 1 }        /* ✗ 남은 높이를 나눠 갖는다 */
```

이러면 **줄 높이 = 카드 높이 ÷ 줄 개수**가 되어, 카드 크기와 줄 수가
페이지마다 다른 만큼 간격도 전부 달라진다.

### 2026-09-23 v8 실측 (브라우저 DOM 기준)

| | 값 |
|---|---|
| 괘선을 쓰는 페이지 | 474 |
| 줄 높이 범위 | **21.3px ~ 139.5px** (6.5배) |
| 한 페이지 안에 2종 이상 | **74페이지** |
| 최악 (`kind`) | 한 장에 21.3 / 25.3 / 33.8 / 60.8 / 60.9px |

반복 페이지(데일리 372·위클리 52)는 `54.8px`·`44px` 로 일정했다. **문제는
고유 템플릿 쪽**이다.

### 할 것

```css
.lines      { flex:1; display:flex; flex-direction:column; min-height:0;
              overflow:hidden }
.lines > div{ height:20pt; flex:none; border-bottom:1px solid var(--line) }
```

**줄은 넘치게 만들고 상자가 잘라낸다.** `lines(n)` 의 `n` 은 하한이고 실제로는
`n + LINE_SPARE`(8) 개를 찍는다. 카드마다 높이가 달라 손으로 맞추면 다음 디자인
변경 때 전부 다시 맞춰야 한다(실측: 고유 페이지 최대 11줄, 데일리 전부 4줄 부족).
잘려 나간 줄은 렌더되지 않으므로 용량은 **16KB** 밖에 늘지 않았다.

### 20pt 를 고른 근거 (26pt 로 갔다가 되돌아왔다)

처음엔 26pt 로 박았더니 **줄이 0~2개뿐인 칸이 33개** 생겼다. 칸은 큰데 줄이
두 개면 "칸 사이가 너무 넓다"는 인상이 된다. 후보를 쓸어봤다:

| 줄 높이 | 0줄 | 1줄 | 2줄 | 3줄 미만 합 |
|---|---|---|---|---|
| 14pt | 0 | 1 | 1 | 2 |
| 16pt | 0 | 1 | 8 | 9 |
| 18pt | 1 | 4 | 8 | 13 |
| **20pt** | **1** | **4** | **11** | **16** |
| 22pt | 4 | 3 | 14 | 21 |
| 26pt | 4 | 12 | 17 | 33 |

14pt 가 수치상 최선이지만 **손글씨가 안 들어간다.** 아이패드 스타일러스로
쓰는 크기를 감안해 `20pt`(26.7px)로 잡았다. 남은 결손은 아래 두 가지를
고쳐서 **0줄 0개**가 됐다.

필기용 괘선이다. **종이 노트는 페이지마다 줄 간격이 바뀌지 않는다.** 간격이
제각각이면 구매자가 칸마다 글씨 크기를 바꿔야 한다.

### 반복 그라데이션은 쓰지 않는다 (시도했다가 물렸다)

`repeating-linear-gradient` 로 그리면 줄 수를 셀 필요가 없어 깔끔해 보인다.
실제로 해봤더니 **파일이 19.23MB → 20.00MB 로 뛰었다.** Etsy 상한이 20MB 라
쓸 수 없다. Chrome 이 **요소마다 타일링 패턴 객체를 하나씩** 박기 때문이고,
Prod 2 가 v9 에서 찾은 것과 같은 원인이다. 테두리 선은 벡터라 훨씬 싸다.

### 뷰어 배율까지 고려할 것

파일 안의 기하가 완벽해도 화면에서 깨질 수 있다. `27pt` 로 박아도 뷰어 배율이
1.5배면 `40.5px` 이 되어 줄마다 반올림이 갈린다. **검사는 정수 배율만으로
통과시키지 않는다 — 1.5 / 2 / 2.5 / 3배에서도 잰다.**

(이 항목은 Prod 2 방이 v9 작업 중 실측으로 확인했다.)

---

## 1-2. 줄 **개수** — 같아 보여야 할 칸은 같은 수가 나와야 한다

간격과는 **다른 결함**이다. 간격이 전부 20pt 로 같아도, 나란히 놓인 카드의
줄 수가 2/3/2/2 로 갈리면 사용자는 즉시 알아챈다. 줄 수는 직접 정하지 않고
`카드 높이 ÷ 20pt` 로 정해지므로, **카드 높이가 1pt 라도 다르면 갈린다.**

### 원인 1 — 힌트 유무가 카드 높이를 바꾼다

`prompt_card(label, hint, ...)` 에서 힌트가 없으면 그 줄이 통째로 사라져
카드가 11pt 높아지고, 그만큼 줄이 하나 더 들어갔다. "Talk to yourself
kindly" 페이지가 2/3/2/2 로 나온 게 이것이다.

```python
# 힌트 슬롯은 힌트가 없어도 자리를 지킨다
h = (f'<div style="... min-height:11pt">{hint or "&nbsp;"}</div>')
```

### 원인 2 — `.row` 로 묶였는지가 카드 높이를 바꾼다

`.row` 에는 `gap` 이 있다. 한 페이지에서 일부 카드만 `.row` 안에 있으면
그 카드들만 `gap` 만큼 낮아져 줄이 하나 적게 들어간다.

- `p_vision` — 위 한 쌍만 `.row` 였다. 아래 두 장도 `.row` 로 묶었다
- `p_rsd` — 카드 6장을 세로로 쌓아 아래 두 장이 **0줄**이었다.
  2장씩 3행으로 바꿨다

### 일부러 다른 것은 놔둔다

`flex` 값을 달리 준 카드는 높이가 달라야 맞다. 2026-09-23 기준 아직
한 페이지 안에서 줄 높이가 갈리는 7장은 **전부 의도된 것**이다:

```
obstacle [3,3,11]   hyperfocus [9,9,9,1,1,1]   doctor [9,9,2,2,2]
boundaries [12,12,11]   therapy [12,12,11]
vision [12,12,11,11]   deadline [9,9,10]
```

**판정 기준: 같은 `flex` 인데 줄 수가 다르면 결함, `flex` 가 다르면 정상.**

---

## 1-3. 섹션 높이는 행 높이의 **배수**여야 한다

간격도 줄 수도 맞췄는데 남는 문제가 하나 더 있다. 줄 높이는 고정인데 섹션
높이는 flex 가 정하므로, 나머지 `H mod 26.7` 이 그대로 **바닥 여백**이 된다.

### 2026-09-23 v8.2 실측

| | 값 |
|---|---|
| 제목 -> 첫 줄 | 0px (균일) |
| 마지막 줄 -> 섹션 바닥 | **0 ~ 220.6px** |
| 8px 미만으로 빡빡한 블록 | 115개 중 **44개** |

바닥이 한 행보다 좁으면 "행이 잘렸다"로 읽힌다.

### CSS 로는 못 없앤다

**CSS 에는 나머지 연산이 없다.** 나머지는 반드시 어딘가로 간다.

나머지를 위로 보내는 시안(`.lines{justify-content:flex-end}`)을 실제로
만들어보고 버렸다(`v8.3-undated`). 바닥은 완벽해지지만 **첫 줄이 제목 쪽으로
잘려** 115개 중 87개가 한 행보다 좁은 필기 공간이 됐다. 바닥이 좁은 것보다 나쁘다.

| | 제목 -> 첫 줄 | 마지막 줄 -> 바닥 |
|---|---|---|
| 위 정렬 (기본) | 0px | 0 ~ 220.6px |
| 바닥 정렬 (v8.3, 폐기) | **-26.8 ~ 0px** | 0px |
| **높이 고정 (v8.4)** | **0px** | **-0.4 ~ 1.6px** |

### 할 것 — 재고 나서 고정한다 (`snap_cards`)

빌드는 HTML 을 쓴 뒤 **헤드리스 Chrome 으로 한 번 재고**, 괘선을 담은 카드를
`제목영역 + k × 행높이` 로 고정하는 스타일시트를 덧붙인 다음 PDF 를 뽑는다.
나머지는 카드 **사이** 간격으로 빠져 읽히지 않는다. 고정은 `data-ln` 을 키로
한 `:has()` 규칙이라 생성된 마크업은 건드리지 않는다.

### 밟은 함정 다섯 (전부 코드에 주석으로 남겼다)

1. **`!important` 가 필요하다.** 카드가 `style="flex:1"` 인라인이라
   덧붙인 스타일시트가 못 이긴다
2. **`flex` 가 어느 축을 정하는지는 부모 방향에 달렸다.** 가로 `.row` 에서
   `flex:none` 을 걸면 **폭**이 글자만큼 쪼그라든다 -- Vision 페이지가 좁은
   기둥 두 개로 렌더됐다. 행에서는 `height` + `align-self`, 열에서는 `flex:none`.
   반대로 열에서 `align-self` 를 걸면 그쪽이 폭을 때린다
3. **줄 수를 나눗셈으로 구하지 마라.** 허용치를 얼마로 줘도 어딘가는 틀린다.
   `0.01` 은 배수보다 살짝 모자란 상자를 "한 줄 적음"으로 읽어 다음 패스에서
   줄을 통째로 깎았고(worry 가 2줄에서 1줄로), `0.15` 는 0.8px 모자란 상자를
   "한 줄 더"로 읽어 카드를 건너뛰었다(meds 에 빈 행이 남았다).
   **실제로 그려지는 줄을 센다.** 허용치 0.5px -- Chrome 의 1/64px 반올림은
   흡수하되 정말 안 들어가는 줄은 제외한다
4. **`min-height:0` 을 같이 건다.** flex 항목의 기본값 `min-height:auto` 는
   내용 높이로 풀리고, 여기서 내용은 잘릴 여분 줄까지다
5. **절대위치 자식은 넘침 검사에서 뺀다.** `position:absolute; inset:0` 은
   카드의 **패딩 박스**를 채우는 것이 의도다. 콘텐츠 박스로 재면 아래 패딩이
   넘침으로 잡힌다(Mind map 의 연결선 svg)

---

## 1-4. 여분 줄은 **높이를 밖에서 정해줄 때만** 찍는다

`lines(n)` 은 `n + LINE_SPARE` 개를 찍고 상자가 잘라낸다. 이건 **카드 높이를
flex 가 정해줄 때만** 성립한다. 높이가 내용에 끌려가는 카드에서는 여분 줄이
잘리는 대신 **카드를 부풀린다.**

두 경우가 있고 둘 다 겪었다:

| 경우 | 증상 | 대응 |
|---|---|---|
| 카드가 `flex:none` | "Working backwards" 의 아래 카드가 10줄로 부풀어 위 입력칸을 짓눌렀다 | `prompt_card` 가 알아서 `spare=0` |
| **행**이 `flex:none` | 시각 하나 적는 `Started`/`Stopped`/`Hours` 가 8줄, 정작 글 쓰는 칸이 1줄 | 카드는 부모를 볼 수 없다. **행이 `spare=0` 을 직접 넘긴다** |

해당 페이지: `hyperfocus`(Started/Stopped/Hours), `doctor`(Date/Who),
`deadline`(Due/Today).

`LINE_SPARE` 를 키우는 것으로 때우려다 물렸다. 8 -> 18 로 올렸더니 높이가
내용에 끌려가는 카드가 334px 에서 575px 로 자라 이웃을 0줄로 만들었다.
**여분은 작게 두고, 줄이 더 필요한 카드는 자기 `lines(n)` 을 올린다.**

---

## 2. 표 (`.trk`) — 바깥 테두리는 없고, 안쪽 구분선만

### 규칙

```
세로 바깥선  없음 (좌우 모두)
가로 마감선  있음 -- 헤더 아래, 그리고 마지막 행 아래
안쪽 구분선  있음
헤더 아래   전 폭을 가로지른다 (첫 칸이 비어 있어도)
```

**가로는 닫고 세로는 연다.** 집 규칙이 "테두리 선 없이 그림자만으로 층을
표현한다"(`CLAUDE.md`)이므로 표를 사각 틀로 두르지는 않는다. 다만 헤더 아래
선이 전 폭을 가로지르는데 마지막 행 아래가 비어 있으면 **표가 끝나지 않은
것처럼 읽힌다**(2026-09-23, 19p Screen time 과 29p Gratitude 에서 지적됨).

`<div>` 로 만든 행 목록(Gratitude 의 요일, Focus session 의 블록 등 9곳)도
같은 규칙이다. 마지막 행만 선을 빼던 `{"" if i == N else ...}` 예외를 없앴다.

### 2026-09-23 에 고친 것

```css
/* 전 */
.trk tr:first-child .nm{border-bottom:none}

/* 후 — 예외를 아예 없앴다 */
.trk td:last-child{border-right:none}
.trk tr:last-child td{border-bottom:none}
```

**증상 1 — 헤더 아래 첫 선이 한 칸에서만 빠짐.**
`.trk` 는 31일 습관 트래커를 위해 만든 것이고, 거기선 첫 칸이 비어 있어 선이
없는 게 맞다. 그 규칙이 `TASK` 라는 진짜 헤더가 있는 표까지 걸렸다.
해당 페이지: `p_estimate`(Guess vs actual), `p_chores`(Who does what).

**증상 2 — 좌우 비대칭.** 라벨 칸(`.nm`)이 `border:none` 이라 왼쪽 바깥선이
없는데, 마지막 칸은 테두리가 있어 오른쪽만 선이 있었다.

**증상 3 — 라벨 칸이 비어 있을 때도 같은 일이 난다.** 처음엔 `:empty` 일 때만
선을 빼는 예외를 뒀는데, 그러면 `screen`·`sleep`·`symptoms`·`movement`·
`cycle`·`habits-grid` 등 **7개 표**에서 헤더 아래 선이 둘째 열부터 시작했다.
예외를 통째로 없애 전 폭을 가로지르게 했다.

> **한 표에 맞춘 셀렉터가 다른 표를 깨뜨린다.** Prod 2 도 같은 함정을 겪었다
> — 4열 표에 맞춘 `nth-child` 규칙 때문에 6열 시간표의 THU·FRI 에 선이
> 안 그려졌다. `.trk` 처럼 여러 표가 공유하는 클래스에 조건을 걸 때는
> **쓰는 표를 전부 세어보고** 건다.

### 달력(`.cal`)은 예외

월 달력은 격자가 닫혀 있는 게 자연스러우므로 바깥 테두리를 유지한다.

---

## 3. 검사

눈으로 보는 검수로는 위 둘 다 놓쳤다. **자동으로 잰다.**

```bash
python scripts/check_lines.py src/planner_v8-undated.html
python scripts/check_lines.py src/planner_v9-student.html   # Prod 2 도 그대로
```

`scripts/check_lines.py` 가 브라우저 DOM 에서 실제 렌더 높이를 읽어
아래를 검사한다:

1. 한 페이지 안에 괘선 높이가 2종 이상인가
2. 문서 전체에서 괘선 높이가 몇 종인가
3. `.trk` 표의 바깥 테두리(우/하)가 남아 있는가
4. 헤더 아래 선이 **열마다 유무가 다른가**
5. **높이가 같은 카드는 줄 수도 같은가** (1-2 절의 규칙 그대로)
6. **내용이 카드를 넘치지 않는가** (1-4 절의 부풀림을 잡는다)
7. **마지막 줄 아래 여유가 0.2px 이상인가** -- 0 이면 래스터에서 줄이
   사라진다. 기준은 실측이다: 여유 0 은 사라졌고 0.42px 는 그려졌다
8. **표의 마지막 행 아래에 선이 있는가**, 그리고 우측 바깥선이 없는가

> 5번을 v8.1 에 걸어봤더니 `vision` 의 12줄 대 11줄을 그대로 집어냈다.
> 사람이 눈으로 찾아낸 결함이고, 규칙으로 적으니 자동으로 잡힌다.

> 4번을 처음엔 "헤더 칸 자신의 `border-bottom` 이 있는가"로 짰는데 오판이
> 났다. `border-collapse` 에서 그 선은 **아래 행의 `border-top`** 에서 올 수도
> 있다. 정상인 `subs` 표가 실패로 잡혔다. 결함은 선의 출처가 아니라
> **열 사이의 불일치**다.

**픽셀을 세지 않는다.** 처음에 렌더 이미지에서 어두운 행을 찾는 방식으로
재봤더니 표 테두리·카드 경계·점자까지 섞여 14~119px 이 나왔다. 쓸 수 없는
측정이었다. **레이아웃은 레이아웃 엔진에게 묻는다.**

---

## 4. 여분 줄이 잘리지 않는 상자도 고정 대상이다

`snap_cards` 가 "잘림이 없으면 건너뛴다"로 판정하면 **상자가 줄보다 넉넉한**
경우를 놓친다. 둘 다 `scrollHeight == clientHeight` 라서 구분이 안 된다.

| 경우 | 판정 |
|---|---|
| 줄 > 상자 (잘림) | 고정한다 |
| 줄 == 상자 (`spare=0`) | 건너뛴다. 높이가 줄에서 나온다 |
| **줄 < 상자 (여유)** | **고정한다.** 이게 공간이 남는 카드다 |

`scrollHeight` 대신 **`줄개수 × 행높이` 와 상자 높이를 직접 비교**한다.
이걸 놓쳐서 19p Screen time 의 두 카드에 한 줄분 빈 공간이 남았다.
